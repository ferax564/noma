# @ferax564/noma-paperdom-react

Host mount for PaperDOM documents. The package is named for the planned React
surface; the published kernel exports a DOM-agnostic `mountPaperDomHost` so
noma-cli does not take a React dependency.
