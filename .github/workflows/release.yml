name: Release

# Publishes @ferax564/noma-cli and @ferax564/noma-mcp-server to npm with
# provenance, and cuts the GitHub release from the CHANGELOG slice.
#
# Two entry points:
#   - push a v* tag (the local-tag-then-push flow from CLAUDE.md), or
#   - run the workflow manually with a `tag` input — CI then creates the tag
#     on the current default-branch HEAD itself (useful when the operator
#     cannot push tags directly).
#
# npm publishing requires an NPM_TOKEN repository secret; when it is absent
# the publish steps are skipped and the tag + GitHub release still happen.

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:
    inputs:
      tag:
        description: Tag to create and release (e.g. v0.14.0); must match package.json
        required: true
        type: string

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Resolve release tag
        id: tag
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            tag="$INPUT_TAG"
          else
            tag="$GITHUB_REF_NAME"
          fi
          case "$tag" in
            v[0-9]*.[0-9]*.[0-9]*) ;;
            *) echo "tag '$tag' is not a vX.Y.Z version tag" >&2; exit 1 ;;
          esac
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Verify tag matches package.json
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          pkg=$(node -p "require('./package.json').version")
          if [ "${TAG#v}" != "$pkg" ]; then
            echo "tag $TAG does not match package.json $pkg" >&2
            exit 1
          fi

      - name: Create tag (manual dispatch only)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          if git ls-remote --exit-code origin "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "tag $TAG already exists on origin — releasing it as-is"
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "$TAG"
            git push origin "refs/tags/$TAG"
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - name: Verify versioned locations
        run: npm run release -- check

      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run verify:conformance
      - run: npm run smoke:package

      - name: Publish @ferax564/noma-cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "NPM_TOKEN secret not configured — skipping npm publish" >&2
            exit 0
          fi
          npm publish --provenance --access public

      - name: Publish @ferax564/noma-mcp-server
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "NPM_TOKEN secret not configured — skipping npm publish" >&2
            exit 0
          fi
          npm publish --provenance --access public -w @ferax564/noma-mcp-server

      - name: Create GitHub release from CHANGELOG slice
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          version="${TAG#v}"
          awk -v ver="$version" '
            $0 ~ "^## \\[" ver "\\]" { found=1; next }
            /^## \[/ { if (found) exit }
            found { print }
          ' CHANGELOG.md > /tmp/notes.md
          gh release create "$TAG" --title "$TAG" --notes-file /tmp/notes.md || \
            echo "release already exists — skipping"
