---
title: Noma — Direction and Positioning
author: ferax564
date: 2026-05-09
tags: [docs, positioning, vision]
---

# Noma — Direction and Positioning

::summary
This document is the canonical statement of what Noma is, what it is not,
and how to evaluate every proposed feature. It refines the original vision
in `PLAN.md` with the post-MVP positioning (PLAN.md §23). Everything here
is itself written in Noma — the rendered HTML version is what you'd send
to a collaborator who has never heard of the project.
::

## The one-liner

::callout{tone="tip"}
**Noma is source-controlled artifacts agents can safely edit.**
::

## The longer version

> Noma is a plain-text source format for durable agent-editable documents. It
> keeps source readable in Git, renders polished artifacts for humans, and gives
> agents stable block IDs so they can patch the exact claim, table, task, or
> citation that changed.

## The three-layer model

::grid{columns=3}
:::card{title="1 · Source" icon="source"}
What humans and agents edit. Plain-text, clean Git diffs, stable over
time, easy to patch at the block level.
:::

:::card{title="2 · Artifact" icon="artifact"}
What humans consume. Standalone HTML by default — visual hierarchy,
grids, cards, charts, review controls, mobile-responsive.
:::

:::card{title="3 · Agent" icon="agent"}
What AI systems use. Deterministic LLM context — more structured than
Markdown, less noisy than HTML — plus IDs, validation, and patch operations.
:::
::

## The central design test

::decision{id="design-test" status="accepted"}
Every feature must answer **yes** to all three:

1. Can a human understand this in raw text?
2. Can an agent edit this safely without destroying the document?
3. Can it render into an artifact people actually want to read?

If any answer is "no", the feature does not belong in core Noma.
::

## Why not Markdown

::claim{id="claim-vs-markdown" confidence=0.85 noverify}
Markdown is excellent as lightweight prose, but it is too weak for
modern AI-era documents. It has no native layout, no semantic blocks,
no validation, no agent patch model, and no clean LLM export.
::

| Need                   | Markdown                | Noma                 |
| ---------------------- | ----------------------- | -------------------- |
| Plain-text readability | Excellent               | Excellent            |
| Git diffs              | Excellent               | Excellent            |
| Grids/cards/tabs       | Poor                    | Native               |
| Charts/plots           | Poor                    | Native               |
| Semantic blocks        | Weak                    | Native               |
| Claims/evidence/risks  | Ad hoc                  | Native               |
| Agent patching         | Weak                    | Native block IDs     |
| Validation             | Weak                    | Built-in             |
| Book scale             | Possible but fragmented | Native project model |
| Beautiful web output   | Needs tooling           | Built-in             |
| LLM context export     | Basic                   | Structured           |

## Why not raw HTML

::claim{id="claim-vs-html" confidence=0.88 noverify}
HTML is excellent as a rendering target, but unpleasant as long-term
source. It is noisy in Git diffs, fragile under agent edits, awkward
for book-scale authoring, and requires custom tooling for every
validation rule.
::

| Need                      | Raw HTML   | Noma                   |
| ------------------------- | ---------- | ---------------------- |
| Browser rendering         | Excellent  | Excellent via renderer |
| Human source readability  | Poor       | Excellent              |
| Long-term maintainability | Poor       | Strong                 |
| Git diffs                 | Noisy      | Clean                  |
| Book authoring            | Awkward    | Native                 |
| PDF/EPUB export           | Awkward    | Native targets         |
| Semantic reasoning blocks | Ad hoc     | Native                 |
| Agent-safe edits          | Fragile    | Block-level operations |
| LLM export                | Noisy      | Clean                  |
| Styling consistency       | Manual CSS | Themes                 |
| Validation                | Custom     | Built-in               |
| Reusable components       | Possible   | Declarative            |

## Five v1 pillars

::grid{columns=2}
:::card{title="1 · Markdown-compatible prose"}
`# headings`, `**bold**`, lists, links, fenced code work without
ceremony. Reduces adoption friction.
:::

:::card{title="2 · Directive blocks"}
`::type{key="value"} ... ::` is the universal extension mechanism.
Examples: `callout`, `claim`, `evidence`, `decision`.
:::

:::card{title="3 · Layout blocks"}
`grid`, `columns`, `card`, `tabs`, `accordion`, `sidebar`, `hero`,
`section`, `callout`. Semantic layout, not CSS.
:::

:::card{title="4 · Data + visualization"}
`dataset`, `plot`, `metric`, `table`. HTML renderer makes them
interactive; PDF static; LLM summarizes.
:::
::

::card{title="5 · Agent collaboration"}
`agent_task`, `instruction`, `review`, `todo`, `decision`, `claim`,
`evidence`, `risk`, `assumption`, `open_question`, `change_request`.
Agent-native from day one.
::

## Artifact-first rendering

A `.noma` source can render into:

- a report
- a spec
- a research memo
- a dashboard
- a landing page
- a prototype
- a code-review explainer
- a book chapter
- a project plan
- a decision record
- a Word-compatible review handoff
- a custom mini-tool

```bash
noma render pr-review.noma --to html --strict --out pr-review.html
```

Output: a single `.html` artifact that opens locally or uploads anywhere. Use `--strict` for team-published artifacts that should block unsafe escape hatches and external runtimes.

## Adoption docs

If you are evaluating Noma rather than extending it, start with these workflow documents:

- [Case studies](case-studies.noma) — concrete research refresh, decision artifact, technical-doc, and agent-memory workflows.
- [Comparison guide](comparison.noma) — when Noma is a better fit than Markdown, MDX, raw HTML, or collaborative docs.
- [Agent editing guide](agent-guide.noma) — the safe patch loop agents should follow.
- [Starter templates](templates.noma) — copyable document shapes for common artifacts.

## Themes, not inline styling

::callout{tone="warning"}
**Avoid:** `::card{padding="24px" border="1px solid #ddd" shadow="large"}`
The source becomes unreadable and the document loses portability.
::

::callout{tone="tip"}
**Prefer:** `::card{variant="important"}`
The theme decides how `important` renders. Source stays clean,
artifacts stay consistent.
::

## Escape hatches

Allowed but not default. Reserved for cases that would otherwise force a
worse compromise.

```noma
::html
<div>Raw HTML only when absolutely necessary.</div>
::

::svg
<svg>...</svg>
::
```

Rules:

- allowed only in artifact mode
- excluded or summarized in LLM mode
- flagged by validator if unsafe
- disabled by default in trusted publishing contexts

## Core directives vs. community packs

::decision{id="core-directive-boundary" status="proposed"}
Core Noma absorbs directives that are **shape-stable, domain-neutral, and useful in more than one downstream renderer**. Domain-specific shapes (finance positions, ops incidents, lab notebooks) belong in **namespaced community packs**, not the core surface.
::

**Why a boundary at all.** The validator and HTML renderer hardcode rules per directive name. Every accepted core directive is a forever-supported shape — it cannot be removed without breaking documents that already use it. A growing surface area also makes the format harder to learn and harder to write tools for.

**Core today (research-shaped).** `claim`, `evidence`, `counterevidence`, `assumption`, `risk`, `hypothesis`, `result`, `limitation`, `open_question`, `decision`, `adr`, `dataset`, `plot`, `metric`, `agent_task`, `todo`, `review`, `comment`, `change_request`, `provenance`, `confidence`, `citation`, `state_change`. These are kept because *any* knowledge-work document benefits from them — a roadmap and a research thesis both need claims, risks, and decisions.

**Out of core (proposed packs).**

| Pack name   | Sample directives                                       | Owner pattern          |
| ----------- | ------------------------------------------------------- | ---------------------- |
| `finance::` | `position`, `trade`, `forecast`, `holding`, `pnl_event` | finance/portfolio docs |
| `ops::`     | `incident`, `runbook`, `postmortem`, `alert`, `oncall`  | SRE/ops teams          |
| `lab::`     | `experiment`, `protocol`, `sample`, `apparatus`         | wet/dry-lab notebooks  |
| `legal::`   | `clause`, `obligation`, `party`, `jurisdiction`         | contract drafting      |

**Pack contract.**

1. **Namespacing.** Pack directive names use `pack::name` (e.g. `::finance::position{...}`). The core parser accepts namespaced directive names; full renderer/validator plug-in loading is still future work.
2. **Renderer plug-in.** Packs ship a function `render(node, ctx) → string` that the core HTML renderer falls back to when it sees an unknown directive name with a registered prefix.
3. **Validator plug-in.** Same shape for validator rules — packs export `validate(node) → Diagnostic[]` for their owned namespace.
4. **No reserved attribute names.** Packs may not redefine `id`, `variant`, `noverify`, `for`, or any attribute the core validator already inspects.
5. **Profile compatibility.** Pack directives are out-of-profile by default. Documents declaring `profile: research | technical | minimal` will warn on pack usage; pack consumers should declare `profile: <pack>` (or omit profile entirely).

**Promotion rule.** A pack directive can be promoted to core if (a) two unrelated packs converge on the same shape, or (b) a renderer/validator rule in the pack would benefit every Noma document. Promotion is a versioned change that ships in a minor release.

This stance means the core stays small, the validator's per-block rule set stays bounded, and the ecosystem can grow without forcing every downstream tool to switch over a sprawling union.

## What NOT to build (yet)

::risk{id="scope-creep" severity="high" owner="ferax564"}
The following are out of scope for v1. Each one would slow the format
work that matters more.

- visual / WYSIWYG editor
- realtime collaboration
- plugin marketplace
- enterprise auth/permissions
- cloud platform
- complex CSS theming engine
- a Markdown-to-Noma converter (Noma → Markdown is the supported direction)
::

## See also

- `PLAN.md` — full vision, including §23 refinements
- `docs/spec.noma` — block-type reference
- `docs/compatibility.noma` — stability and schema policy
- `docs/agent-protocol.noma` — patch protocol
- `examples/agent-plan.noma` — Demo 1
- `examples/tech-doc.noma` — Demo 2
- `examples/research-thesis.noma` — Demo 3

::agent_task{id="direction-doc-review"}
On every material change to `PLAN.md` §23, re-read this document and
update the affected sections. Keep the one-liner, three-layer model,
and central design test in lockstep with PLAN.md.
::
